# temp.civet

import {mkTempFile, mkTempFileSync} from 'base'

# ---------------------------------------------------------------------------

path := mkTempFileSync '.civet'
Deno.writeTextFileSync path, "Hello"
str := Deno.readTextFileSync path
console.log str
console.log "file at #{path}"
