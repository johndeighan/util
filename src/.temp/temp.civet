# temp.civet

import {allStackFrames, dumpFrame, newerDestFileExists} from 'base'
import {stdChecks} from 'llutils'
import {setDirTree} from 'unit-test'

stdChecks 'temp'

# ---------------------------------------------------------------------------

path := 'src/test/base/file.txt'
result := newerDestFileExists(path, '.tsx')
console.log "result = #{result}"
