# cielo.lib.test.civet

import {defined} from 'base'
import {isFunction} from 'datatypes'
import {
	cielo2civet, cielo2civetFile,
	} from 'cielo'
import {
	equal, succeeds, fails, truthy, falsy, setDirTree,
	} from 'unit-test'

# ---------------------------------------------------------------------------
# ASYNC

setup := (): void ->

	await setDirTree """
		./src/test/cielo clear
		file.config.ts
			export default new Object({
				a: 1,
				b: 'abc',
				f: () => 'hello'
				})
		"""
	return

await setup()

# ---------------------------------------------------------------------------

equal await cielo2civet('abc'), 'abc'
