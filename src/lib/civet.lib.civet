# civet.lib.civet

import {exists, existsSync} from '@std/fs'
import {stat, statSync} from 'node-fs'
import {Node, SourceFile} from 'npm-typescript'
import {RawSourceMap} from 'npm-source-map'

import {
	undef, defined, notdefined, hash, assert, isString, isHash,
	isEmpty, nonEmpty, croak, getErrStr,
	} from 'datatypes'
import {getOptions, o, rtrim} from 'llutils'
import {OL, ML} from 'to-nice'
import {LOG, DBG, ERR, DBGVALUE} from 'logger'
import {flag, debugging, inspecting} from 'cmd-args'
import {
	isFile, fileExt, withExt, slurp, slurpAsync, pathStr, touch,
	barf, barfTempFile, parsePath, addJsonValue, normalizePath,
	} from 'fsys'
import {
	execCmdSync, execCmd, CFileHandler, procFiles, TExecResult,
	} from 'exec'
import {ts2ast, analyzeTS, typeCheckTsCode} from 'typescript'
import {extractSourceMap, haveSourceMapFor} from 'source-map'

import hCivetConfig from "civetconfig" with {type: "json"};

# ---------------------------------------------------------------------------

export type TCivetOptions = {
	force?: boolean
	nocheck?: boolean
	inlineMap?: boolean
	}

# ---------------------------------------------------------------------------
# --- Due to a bug in either the v8 engine or Deno,
#     we have to generate, then remove the inline source map,
#     saving it to use in mapping source lines later

class CCivetCompiler extends CFileHandler

	get op()
		return 'doCompileCivet'

	# ..........................................................
	# ASYNC

	override handle(
			path: string,
			hOptions: hash = {}
			): TExecResult

		{force, nocheck} := getOptions<TCivetOptions> hOptions, {
			force: false
			nocheck: false
			}

		assert (fileExt(path) == '.civet'), "Not a civet file: #{path}"
		tsPath := withExt path, '.ts'

		# --- Check if a newer compiled version already exists
		if (
				   not force
				&& await exists(tsPath)
				&& (statSync(tsPath).mtimeMs > statSync(path).mtimeMs)
				&& haveSourceMapFor(tsPath)
				)
			return {
				path
				success: true
				notNeeded: true
				stdout: ''
				stderr: ''
				}

		try
			hResult := await execCmd 'deno', [
				'run', '-A'
				'@danielx/civet'
				'--config', 'C:/Users/johnd/civetconfig.json'
				'--inline-map'
				'-o', '.ts'
				'-c', path
				]
			if not hResult.success
				console.log @getOutput(hResult)
				croak "Compile failed"

			unless nocheck
				hCheckResult := await execCmd 'deno', ['check', tsPath]
				if not hCheckResult.success
					touch path
					console.log @getOutput(hCheckResult)
					croak "Type check failed"

			tsCode := await slurpAsync tsPath
			[code, hSrcMap] := extractSourceMap(tsCode)
			if defined(hSrcMap)
				addJsonValue 'sourcemaps.json', normalizePath(tsPath), hSrcMap
			await Deno.writeTextFile tsPath, code
			return {
				path,
				success: true
				stdout: ''
				stderr: ''
				}

		catch err
			if debugging
				LOG getErrStr(err)
			errMsg := "COMPILE FAILED: #{pathStr(path)} - #{getErrStr(err)}"
			return {
				path
				success: false
				stdout: ''
				stderr: errMsg
				}

	# ..........................................................
	# SYNC

	handleSync(
			path: string,
			hOptions: hash = {}
			): TExecResult

		{force, nocheck} := getOptions<TCivetOptions> hOptions, {
			force: false
			nocheck: false
			}

		assert (fileExt(path) == '.civet'), "Not a civet file: #{path}"
		tsPath := withExt path, '.ts'

		# --- Check if a newer compiled version already exists
		if (
				   not force
				&& existsSync(tsPath)
				&& (statSync(tsPath).mtimeMs > statSync(path).mtimeMs)
				&& haveSourceMapFor(tsPath)
				)
			return {
				path
				success: true
				notNeeded: true
				stdout: ''
				stderr: ''
				}

		try
			hResult := execCmdSync 'deno', [
				'run', '-A'
				'@danielx/civet'
				'--config', 'C:/Users/johnd/civetconfig.json'
				'--inline-map'
				'-o', '.ts'
				'-c', path
				]
			if not hResult.success
				console.log @getOutput(hResult)
				croak "Compile failed"

			unless nocheck
				hCheckResult := execCmdSync 'deno', ['check', tsPath]
				if not hCheckResult.success
					console.log @getOutput(hCheckResult)
					croak "Type check failed"

			tsCode := slurp tsPath
			[code, hSrcMap] := extractSourceMap(tsCode)
			if defined(hSrcMap)
				addJsonValue 'sourcemaps.json', normalizePath(tsPath), hSrcMap
			Deno.writeTextFileSync tsPath, code
			return {
				path
				success: true
				stdout: ''
				stderr: ''
				}

		catch err
			if debugging
				LOG getErrStr(err)
			errMsg := "COMPILE FAILED: #{pathStr(path)} - #{getErrStr(err)}"
			return {
				path
				success: false
				stdout: ''
				stderr: errMsg
				}

export doCompileCivet := new CCivetCompiler()

# ---------------------------------------------------------------------------
# SYNC

export civet2tsFile := (
		civetPath: string,
		hOptions: hash = {}
		): string =>

	hResult: TExecResult := doCompileCivet.handleSync(civetPath, hOptions)
	{success} := hResult
	if success
		return withExt(civetPath, '.ts')
	else
		output := hResult.stdout + '\n' + hResult.stderr
		errMsg := "compile of #{civetPath} failed\n#{output}"
		ERR errMsg
		throw errMsg

# ---------------------------------------------------------------------------

export civet2ts := (
		civetCode: string,
		hOptions: hash = {}
		path: string? = undef
		): string =>

	tempPath := barfTempFile civetCode, {ext: '.civet'}
	tsPath := civet2tsFile tempPath, hOptions
	tsCode := slurp tsPath
	return rtrim(tsCode)

# ---------------------------------------------------------------------------

export civet2ast := (civetCode: string): Node =>

	tsCode := civet2ts civetCode
	return ts2ast tsCode

# ---------------------------------------------------------------------------
# ASYNC

export compileAllLibs := (
		hOptions: hash = {}
		): TExecResult[] =>

	# --- with 'quiet' option, still reports errors
	return await procFiles [doCompileCivet, ['**/*.lib.civet']], {
		...hOptions
		quiet: true
		}

# ---------------------------------------------------------------------------

# --- template literals to simplify displaying
#     the analysis of civet code

export a := (lStrings: TemplateStringsArray): string =>
	tsCode := civet2ts lStrings[0], {nocheck: true}
	result := analyzeTS(tsCode).asString()
	return result

export A := (lStrings: TemplateStringsArray): string =>
	tsCode := civet2ts lStrings[0], {nocheck: true}
	result := analyzeTS(tsCode, {dump: true}).asString()
	return result
